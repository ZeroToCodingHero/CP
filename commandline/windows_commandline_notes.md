new-item / touch = create file
rm = remove file
mkdir = make directory / folder
rmdir = remove directory
cd = change directory / cd .. or cd ../..
control L = cls = clear 
control / = muilti line #
ls = list of files in folder
cp = copy a file or directory
pwd = print working directory

move / open = view a file
mv = move / rename
^ = scroll through previous commands
echo "This is a test file." > test.txt
cat test.txt = prints out the context of the file
help = finding help
hostname = network name
exit = exiting your terminal
dir-r = finding files


ping -n 100 8.8.8.8 or 1.1.1.1 or 9.9.9.9
ping -n 100 192.168.x.x

pathping 1.1.1.1
